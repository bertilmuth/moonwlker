import java.math.BigDecimal;

import org.requirementsascode.moonwlker.testobject.animal.Dog;

public class StrayDog extends Dog {
  public StrayDog(BigDecimal price, String name, String command) {
    super(price, name, command);
  }
}